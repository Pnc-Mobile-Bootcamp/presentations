
# rename the current branch from "master" to "main":
git branch -m master main


# change the setting so new repos get created with "main":
git config --global init.defaultBranch main













# creates a new branch (profile-feature) based on the main branch
# and then checks it out
	git checkout -b profile-feature main

































































































